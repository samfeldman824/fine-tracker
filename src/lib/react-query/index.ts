// Export the client and query keys
export { queryClient, queryKeys } from './client'

// Export the provider
export { ReactQueryProvider } from './provider'

// Export all hooks
export * from './hooks'